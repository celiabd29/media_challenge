{
  "compilerOptions": {
    "baseUrl": ".", // Cela définit la racine du projet comme base pour les chemins
    "paths": {
      "@/*": ["src/*"] // Cela configure l'alias "@" pour pointer vers le dossier "src"
    }
  }
}
